import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <section className={`flex flex-col px-6 gap-y-6 pt-12 pb-6`}>
      <LoginForm />
    </section>
  );
};

export default LoginPage;
